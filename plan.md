# WPF プログラムランチャー実装計画（Codex Cloud 向け）

## 1. 目的

`draft.md` の要件を、Codex Cloud で小さく実装・検証しながら完成させるための作業計画を定義する。

最初のリリース（フェーズ1）では、XML 設定を読み込んで「カテゴリー → ボタン」の2階層でプログラムを起動できる Windows デスクトップアプリを作る。認証機能はフェーズ1の設計を壊さず追加できる境界だけを先に用意し、外部ページを使う認証とセキュア情報の払い出しはフェーズ2で実装する。

## 2. 前提と判断事項

### 2.1 確定要件

- クライアントは .NET Framework 4.6.2、VB.NET、WPF（XAML）で実装する。
- ランチャー設定は XML ファイルに保存する。
- 画面はカテゴリー一覧と、選択カテゴリーに属するボタン一覧の2階層とする。
- 各ボタンには任意の画像を表示できる。
- シンプルでモダンな外観にする。
- Windows の GitHub-hosted runner でビルド、テスト、画面キャプチャを実行し、結果を GitHub Actions のアーティファクトとして保存する。
- フェーズ2では、.NET 10 の Razor Pages と Minimal API を利用する認証サービスを追加する。

### 2.2 実装前に明確化する仮定

以下は `draft.md` だけでは一意に決まらないため、フェーズ1では次の既定値を採用する。異なる要望が判明した場合は、実装前に本節と受け入れ条件を更新する。

- 対象 OS は Windows 10/11 とし、x64 を主要ターゲットにする。
- 設定ファイルは実行ファイルと同じディレクトリの `launcher.config.xml` を既定とし、起動引数 `--config <path>` で上書き可能にする。
- カテゴリーとボタンの並び順は XML の `order`、同値の場合は記述順で決定する。
- ボタン画像は PNG/JPEG/ICO を対象とし、相対パスは設定ファイルのディレクトリを基準に解決する。
- 起動対象、画像、作業ディレクトリの各パスでは環境変数を展開する。
- 画像がない、または読み込めない場合は既定アイコンを表示し、アプリ全体は停止させない。
- Razor Pages / Minimal API は .NET 10 の標準テンプレートとの整合性を優先して C# で実装する。WPF クライアントは VB.NET のままとする。
- 「URL スキームによるトークンの付与」は、Bearer トークンそのものを URL に含めず、短時間・一回限りの認可コードをカスタム URI で返し、バックチャネルでトークンに交換する方式として実装する。

## 3. 対象範囲

### 3.1 フェーズ1に含めるもの

1. WPF アプリケーションの雛形と MVVM による責務分離。
2. XML 設定の読み込み、XSD 検証、モデルへの変換。
3. カテゴリー選択、ボタン表示、画像表示、外部プログラム起動。
4. 不正設定や起動失敗を利用者へ伝えるエラー表示とログ。
5. サンプル設定と設定仕様書。
6. 単体テスト、設定統合テスト、Windows UI スモークテスト。
7. GitHub Actions による Windows ビルド、テスト、ログ・スクリーンキャプチャの保存。

### 3.2 フェーズ1に含めないもの

- GUI 上での設定編集。
- 自動更新、インストーラー、コード署名。
- 管理者権限への自動昇格。
- リモートからの設定同期。
- 外部ページ認証とセキュア情報の払い出し（フェーズ2で実施）。

## 4. 推奨リポジトリ構成

```text
/
├─ .github/workflows/
│  ├─ ci.yml                         # ビルド、単体テスト、成果物保存
│  └─ ui-smoke.yml                   # 対話セッション不要のUI起動・撮影検証
├─ docs/
│  ├─ configuration.md               # XML設定の利用者向け仕様
│  ├─ development.md                 # ローカル/Codex Cloud開発手順
│  └─ authentication-phase2.md       # フェーズ2の脅威モデルとAPI契約
├─ schemas/
│  └─ launcher-config.xsd
├─ samples/
│  ├─ launcher.config.xml
│  └─ images/
├─ src/
│  ├─ Launcher.App/
│  │  ├─ App.xaml
│  │  ├─ App.xaml.vb
│  │  ├─ Models/
│  │  ├─ Services/
│  │  ├─ ViewModels/
│  │  ├─ Views/
│  │  └─ Resources/
│  └─ Launcher.Core/                 # WPF非依存の設定・起動ロジック
├─ tests/
│  └─ Launcher.Core.Tests/
├─ tools/
│  ├─ capture-window.ps1
│  └─ verify-artifacts.ps1
├─ Launcher.sln
├─ draft.md
└─ plan.md
```

`Launcher.Core` は .NET Framework 4.6.2 のクラスライブラリとし、UI を起動せず Linux 上でも静的確認しやすい構造にする。ただし正式なビルドとテスト結果は Windows runner を正とする。

## 5. アーキテクチャ方針

### 5.1 クライアント

- **View**: XAML の表示とバインディングだけを担当し、コードビハインドはウィンドウ固有の処理に限定する。
- **ViewModel**: 選択中カテゴリー、表示対象ボタン、起動コマンド、エラー通知を保持する。
- **Model**: `LauncherConfiguration`、`CategoryDefinition`、`ButtonDefinition` を不変に近いデータとして表す。
- **設定サービス**: XML を安全な設定の `XmlReader` で読み、XSD 検証後にモデルへ変換する。DTD と外部エンティティは無効化する。
- **起動サービス**: 実行ファイル、引数、作業ディレクトリを `ProcessStartInfo` に個別設定する。シェル文字列の連結や `cmd.exe /c` は行わない。
- **画像サービス**: 相対パス解決、読み込み失敗時のフォールバック、ファイルをロックし続けない読み込みを担当する。
- **ログサービス**: 日時、重大度、処理名、例外概要を `%LOCALAPPDATA%/<製品名>/Logs` に保存する。認証情報、トークン、セキュア情報は記録しない。

依存方向は `View → ViewModel → Service interface / Model` とし、ファイルアクセスとプロセス起動をインターフェース越しに差し替えて単体テスト可能にする。

### 5.2 XML 設定の初期案

```xml
<?xml version="1.0" encoding="utf-8"?>
<launcher title="業務ランチャー" version="1">
  <categories>
    <category id="daily" name="日常業務" order="10">
      <button id="calculator"
              name="電卓"
              order="10"
              executable="%WINDIR%\System32\calc.exe"
              arguments=""
              workingDirectory="%WINDIR%\System32"
              image="images\calculator.png" />
    </category>
  </categories>
</launcher>
```

XSD と読み込み処理では、必須属性、ID の重複、順序値、空カテゴリー、存在しないファイルを検証する。構文・スキーマ違反は起動を止めて場所を示し、個別画像の欠落は警告、起動対象の欠落は該当ボタンを無効化する。

### 5.3 UI 方針

- 左側にカテゴリー、右側にレスポンシブなボタングリッドを配置する。
- 色、余白、角丸、文字サイズ、フォーカス表示は `ResourceDictionary` に集約する。
- マウスだけでなく Tab、矢印、Enter/Space で操作可能にする。
- 高 DPI、100%/150% の表示倍率、長い日本語ラベル、画像なしの状態を確認する。
- 色だけに依存せず、エラー文と無効状態を明示する。
- UI テストで要素を特定できるよう AutomationId を付与する。

## 6. Codex Cloud での実装手順

各ステップは原則として独立した小さなコミット（または小さな PR）にし、「変更 → ローカルで可能な検証 → push → Actions の完了待ち → アーティファクト確認 → 必要なら修正」を繰り返す。

### ステップ0: ベースラインと外部仕様の確認

1. `AGENTS.md`、`draft.md`、本計画を読む。
2. 作業開始時に `git status --short --branch` を確認し、利用者の未コミット変更を上書きしない。
3. Microsoft Learn で .NET Framework 4.6.2 Developer Pack、WPF、MSBuild、.NET 10 の Razor Pages / Minimal API の最新手順を確認する。
4. GitHub Docs と各 Action の公式リポジトリで、Windows runner の利用可能なイメージ、権限、`actions/checkout`、`actions/upload-artifact`、`actions/setup-dotnet` の現行メジャーバージョンと breaking changes を確認する。
5. 確認日、採用バージョン、参照 URL を `docs/development.md` に記録する。`windows-latest` の内容に暗黙依存せず、必要な Visual Studio/MSBuild コンポーネントをログへ出す。

完了条件: 採用するプロジェクト形式、MSBuild コマンド、テストランナー、Action バージョンが文書化されている。

### ステップ1: ソリューションと最小起動アプリ

1. `Launcher.sln`、VB.NET の `Launcher.App` と `Launcher.Core`、テストプロジェクトを作る。
2. 全プロジェクトのターゲットを .NET Framework 4.6.2 に統一する。
3. 最小の `App.xaml` と `MainWindow` を作り、サンプルデータでウィンドウを表示する。
4. 日本語のモジュールヘッダーコメントと関数ヘッダーコメントを追加し、複雑な判断には細かな日本語コメントを付ける。
5. Release/x64 と Debug/Any CPU のビルドを確認する。

完了条件: クリーンな Windows runner で restore と build が成功し、アプリが例外なく起動・終了する。

### ステップ2: XML 設定とドメインモデル

1. XML のバージョン付きスキーマを作成する。
2. DTD/外部エンティティを禁止した設定ローダーを実装する。
3. パス解決と環境変数展開を専用サービスに分離する。
4. 正常、必須値不足、重複 ID、不正 order、XXE 入力、画像欠落、実行ファイル欠落のテストを追加する。
5. サンプル設定と `docs/configuration.md` をスキーマに同期させる。

完了条件: 正常なサンプルがモデルへ変換され、不正入力ごとに利用者が修正できるエラーメッセージを返す。

### ステップ3: MVVM と画面実装

1. カテゴリー選択とボタン絞り込みを ViewModel に実装する。
2. コマンド実装を追加し、実行中の二重操作を防止する。
3. 画像の非同期または遅延読み込みと既定画像へのフォールバックを実装する。
4. テーマ用 ResourceDictionary、キーボードフォーカス、AutomationId を追加する。
5. 画像なし、長い名称、多数ボタン、空カテゴリーの表示を確認する。

完了条件: 主要操作をキーボードだけで完了でき、ViewModel の分岐を UI 非依存のテストで確認できる。

### ステップ4: 安全なプログラム起動と障害処理

1. 起動サービスを実装し、実在確認、作業ディレクトリ、引数、終了を待たない通常起動を扱う。
2. 設定値をコマンドシェルへ渡さず、意図しないコマンド連結を防ぐ。
3. 起動成功、対象なし、アクセス拒否、不正な作業ディレクトリをモックまたはテスト用実行ファイルで確認する。
4. 利用者向けダイアログには簡潔な説明とログ参照先を表示し、ログには診断可能な例外情報を残す。

完了条件: 1つの起動失敗でランチャーが終了せず、同じセッションで別ボタンを操作できる。

### ステップ5: GitHub Actions の検証パイプライン

1. `pull_request` と既定ブランチへの `push` で動く `ci.yml` を作成する。
2. workflow の既定権限を `contents: read` に絞り、依存 Action は検証済みのコミット SHA に固定する。
3. Windows runner で環境情報、restore、Release build、テストを順に実行する。
4. テスト結果（TRX）、MSBuild バイナリログ、アプリ配布用 ZIP を、成功・失敗にかかわらず適切な保持期間で保存する。
5. `ui-smoke.yml` でサンプル設定を使ってアプリを起動し、対象ウィンドウが現れるまで有限時間ポーリングする。
6. `tools/capture-window.ps1` で対象ウィンドウを撮影し、PNG、アプリログ、スクリプトログを保存する。失敗時も診断用成果物をアップロードする。
7. 秘密値を fork 由来 PR へ公開しない。フェーズ1の workflow は secrets なしで完結させる。

完了条件: 新規 checkout から人手操作なしでビルド・テストが成功し、Actions の run から ZIP、TRX、binlog、PNG、ログを取得できる。

### ステップ6: Codex Cloud からの結果確認ループ

GitHub CLI が利用できる場合は、対象ブランチを push した後に次の流れで確認する。実際の workflow 名と run ID は固定値にせず取得結果を使用する。

```powershell
gh workflow run ci.yml --ref <branch>
gh run list --branch <branch> --limit 5
gh run watch <run-id> --exit-status
gh run download <run-id> --dir artifacts/<run-id>
powershell -File tools/verify-artifacts.ps1 -Path artifacts/<run-id>
```

- `gh` の認証や push 権限がない環境では、コードと workflow の作成までを行い、PR の必須チェックとして Actions を起動する。
- 失敗時はまずテキストログと TRX/binlog を解析し、表示上の問題だけをスクリーンキャプチャで判断する。
- キャプチャは画像サイズ、非単色、対象ウィンドウタイトルのログを機械検証し、デスクトップ全体や無関係なウィンドウを成果物へ含めない。
- 修正後は失敗したジョブだけでなく、最終的に workflow 全体を再実行する。

### ステップ7: フェーズ1の仕上げ

1. Release ビルドの出力にサンプル設定と必要ファイルだけを含める。
2. 初回起動、設定エラー、起動成功、起動失敗の手順を README に記載する。
3. 受け入れ条件をすべて再確認し、既知の制約を記載する。
4. インストーラーや署名が必要になった場合は、別フェーズとして配布方式と証明書管理を設計する。

## 7. テスト計画

### 7.1 自動テスト

| レベル | 主な確認内容 | 実行場所 |
|---|---|---|
| 単体 | XML 変換、検証、パス解決、並び順、ViewModel、コマンド状態 | Windows runner |
| セキュリティ | DTD/XXE 拒否、巨大・深過ぎる入力の制限、シェル非経由起動、ログの機密情報除外 | Windows runner |
| 統合 | サンプル XML から画面モデル構築、テスト用プロセス起動、ログ出力 | Windows runner |
| UI スモーク | 起動、カテゴリー選択、ボタン表示、スクリーンキャプチャ、正常終了 | Windows runner |
| 成果物 | ZIP/TRX/binlog/PNG/ログが存在し、空でなく、想定形式であること | Windows runner |

### 7.2 手動確認

- Windows 10/11、表示倍率 100%/150% でレイアウトが崩れない。
- キーボードのみでカテゴリー選択とボタン起動ができる。
- 長い日本語名や大量のボタンでも操作可能である。
- 設定ファイル、画像、起動対象が欠けた場合の案内が理解できる。
- ログ、スクリーンキャプチャ、Actions ログに個人情報や秘密値が含まれない。

## 8. フェーズ1の受け入れ条件

- 有効な XML を読み、カテゴリーを選ぶと所属ボタンだけが order 順に表示される。
- 画像ありのボタンは指定画像、画像なし・破損時は既定画像を表示する。
- 有効なボタンを操作すると、指定された引数と作業ディレクトリでプログラムが一度だけ起動する。
- 不正な XML は、ファイル名・可能なら行/列・修正理由を含む日本語メッセージで拒否される。
- 起動不能なボタンは安全に失敗し、ランチャーの利用を継続できる。
- マウスとキーボードの双方で主要機能を操作できる。
- Windows の GitHub Actions で restore、build、test、UI スモークが成功する。
- run ごとに配布 ZIP、テスト結果、ビルドログ、画面 PNG、アプリログを取得できる。
- ソースの各モジュールと関数に、日本語の目的・前提・副作用を示すヘッダーコメントがある。

## 9. フェーズ2: 外部ページ認証

フェーズ1完了後、実装前に `docs/authentication-phase2.md` で脅威モデルとデータ分類をレビューする。

### 9.1 構成

- .NET 10 の ASP.NET Core アプリにログイン用 Razor Pages とコード交換・情報払い出し用 Minimal API を配置する。
- WPF 側には `IAuthenticationService` と `ISecureDataService` の境界を追加する。
- OS の既定ブラウザーで HTTPS の認証ページを開き、認証後に `product-launcher://auth/callback?code=...&state=...` へ戻す。
- カスタム URI の登録は配布工程で行い、アプリ起動時に受け取った URI の scheme、host、path、state を厳密に検証する。

### 9.2 セキュリティ要件

- URL にはアクセストークンやセキュア情報を載せず、一回限り・短寿命・利用クライアントに紐づく認可コードだけを載せる。
- 認可要求ごとに暗号学的に安全な `state` と PKCE の verifier/challenge を生成する。
- コード交換と情報払い出しは TLS 上で行い、issuer、audience、署名、期限、not-before を検証する。
- リプレイ防止のため認可コードを原子的に一度だけ消費し、失敗回数と有効期限を制限する。
- 長期秘密をクライアントへ埋め込まない。保存が必要な更新トークンは Windows DPAPI などユーザー単位の保護領域へ保存する。
- トークン、コード、払い出した情報を URL、例外、通常ログ、Actions artifact に記録しない。
- CORS、CSRF、Cookie 属性、レート制限、鍵ローテーション、失効、監査ログを設計する。
- GitHub Actions では OIDC など短期資格情報を優先し、長期クラウド秘密をリポジトリへ保存しない。

### 9.3 フェーズ2の作業順

1. 認証方式、IdP、払い出す情報、保存期間、失効要件を決定する。
2. 公式の .NET 10 / ASP.NET Core / 利用 IdP ドキュメントを確認し、API 契約と脅威モデルを確定する。
3. ローカルの偽 IdP を用意し、正常、state 不一致、期限切れ、コード再利用、ユーザー拒否を自動テストする。
4. Razor Pages のログイン画面と Minimal API のコード交換を実装する。
5. WPF のカスタム URI 受信、単一起動制御、PKCE、トークン保護を実装する。
6. セキュア情報 API を最小権限で実装し、認可ポリシーをテストする。
7. ステージング環境で E2E テストを行い、秘密を除外した結果だけを artifact に保存する。

フェーズ2完了条件: 正常な認証後だけ必要最小限の情報を取得でき、改ざん、リプレイ、state 不一致、期限切れ、キャンセル時には情報を返さず、安全に再試行できる。

## 10. リスクと対策

| リスク | 対策 |
|---|---|
| .NET Framework 4.6.2 が古く runner 更新の影響を受ける | Developer Pack と MSBuild の利用可否を開始時と定期的に確認し、環境情報と binlog を保存する |
| `windows-latest` の更新で結果が変わる | 必要に応じて特定 Windows イメージを明示し、移行 PR で更新する |
| hosted runner の画面セッション差により撮影が不安定 | 有限タイムアウト、ウィンドウ特定、診断ログ、再現可能なサンプル設定を用意する |
| 外部プログラム起動が任意コード実行経路になる | 信頼されたローカル設定だけを扱い、シェルを介さず、将来のリモート設定には署名検証を必須とする |
| XML や画像によるメモリ/外部参照問題 | DTD/外部エンティティ禁止、サイズ・深さ上限、画像寸法上限を設定する |
| URL スキームが別アプリに奪われる | URL 内に秘密を入れず、state、PKCE、一回限りコード、短い期限で影響を抑える |
| Actions のサプライチェーン侵害 | workflow 権限を最小化し、第三者 Action を避け、利用 Action をコミット SHA に固定する |

## 11. 実装中のチェックリスト

- [ ] 作業前に対象ディレクトリの `AGENTS.md` を再確認した。
- [ ] 外部サービスや Action を変更する前に公式の最新ドキュメントを確認した。
- [ ] 変更は1つの目的に絞り、不要な整形や生成物を含めていない。
- [ ] VB.NET のモジュール・クラス・関数に日本語のヘッダーコメントを付けた。
- [ ] 複雑な処理とセキュリティ上の判断に日本語コメントを付けた。
- [ ] 秘密値、トークン、個人情報をソース、ログ、画像、artifact に含めていない。
- [ ] ローカルで可能な静的確認とテストを行った。
- [ ] Windows Actions の全ジョブが成功した。
- [ ] TRX、binlog、PNG、アプリログを確認した。
- [ ] 新しい挙動と既知の制約を文書化した。

## 12. 完成の定義

フェーズごとの受け入れ条件を満たし、クリーンな Windows runner で再現可能なビルド・テスト・スクリーンキャプチャが成功し、利用手順・設定仕様・制約・セキュリティ判断が文書化された時点を完成とする。テストをスキップした場合や artifact を目視できない場合は完成扱いにせず、理由と残作業を PR に明記する。
